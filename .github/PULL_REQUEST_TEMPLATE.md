## What & why

<!-- What does this PR change, and what problem does it solve? Link the issue if one exists. -->

Closes #

## Checklist

- [ ] `npm run lint`, `npm test`, and `npm run build` all pass locally
- [ ] New behavior has a corresponding test (especially for `lib/parser/` changes)
- [ ] No unrelated changes bundled in
- [ ] Security-sensitive changes (middleware, `app/api/**`, RLS, auth) explain the access-control reasoning above
